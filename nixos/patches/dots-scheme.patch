--- a/hypr/hyprland.lua	2026-07-31 18:00:15.251197878 +0200
+++ b/hypr/hyprland.lua	2026-07-31 18:00:15.252419147 +0200
@@ -1,6 +1,6 @@
 local home   = os.getenv("HOME")
 local hypr   = home .. "/.config/hypr"
-package.path = package.path .. ";" .. home .. "/.config/caelestia/?.lua"
+package.path = package.path .. ";" .. home .. "/.config/caelestia/?.lua" .. ";" .. home .. "/.config/caelestia/scheme/?.lua"
 
 -- Create a file if it doesn't exist, optionally with initial content
 local function maybe_create(file, content)
@@ -37,8 +37,11 @@
     input:close()
 end
 
--- Maybe set current colours to defaults
-maybe_copy(hypr .. "/scheme/default.lua", hypr .. "/scheme/current.lua")
+-- Maybe set current colours to defaults. The dots config dir is read-only
+-- under NixOS (home-manager store symlink), so write the user scheme to the
+-- writable caelestia config dir and expose it to require().
+os.execute("mkdir -p " .. home .. "/.config/caelestia/scheme")
+maybe_copy(hypr .. "/scheme/default.lua", home .. "/.config/caelestia/scheme/current.lua")
 
 -- User variables
 maybe_create(home .. "/.config/caelestia/hypr-vars.lua", "return {}\n")
